{
  "name": "garp-docs",
  "version": "1.0.0",
  "description": "Documentation for the GARP (Generally Accepted Recordkeeping Principles) language",
  "scripts": {
    "start": "pnpm exec eleventy --serve",
    "build": "pnpm exec eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.0",
    "markdown-it": "^14.0.0",
    "markdown-it-anchor": "^8.6.7"
  }
}
